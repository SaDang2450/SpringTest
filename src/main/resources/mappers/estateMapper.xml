<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
	namespace="com.sadang.spring.test.mybatis.repository.EstateRepository">

	<select id="selectEstateId" parameterType="int"
		resultType="com.sadang.spring.test.mybatis.domain.Estate">
	
		SELECT
			`id`
			,`realtorId`
			,`address`
			,`area`
			,`type`
			,`price`
			,`rentPrice`
			,`createdAt`
			,`updatedAt`
		FROM
			`real_estate`
		WHERE
			`id` = #{id}
		
	</select>

	<select id="selectEstateRent"
		resultType="com.sadang.spring.test.mybatis.domain.Estate">
	
		SELECT
			`id`
			,`realtorId`
			,`address`
			,`area`
			,`type`
			,`price`
			,`rentPrice`
			,`createdAt`
			,`updatedAt`
		FROM
			`real_estate`
		WHERE
			`rentPrice` &lt; #{rent}
		
	</select>
	
	<select id="selectEstateAreaPrice"
		resultType="com.sadang.spring.test.mybatis.domain.Estate">
		
		SELECT
			`id`
			,`realtorId`
			,`address`
			,`area`
			,`type`
			,`price`
			,`rentPrice`
			,`createdAt`
			,`updatedAt`
		FROM
			`real_estate`
		WHERE
			`area` &gt;= #{area} AND `price` &lt;= #{price}
		ORDER BY `createdAt` DESC
			
	</select>
</mapper>